/**
 * Returns the relative size in `em` units for a given value in pixels.
 * @param px value in pixels
 * @param fontSize font size in pixels
 * @returns relative size in `em` units
 */
export default function px2em(px: number, fontSize: number): string | null;
//# sourceMappingURL=px2em.d.ts.map