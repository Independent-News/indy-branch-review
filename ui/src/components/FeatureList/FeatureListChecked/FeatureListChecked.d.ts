import { ReactNode } from 'react';
type FeatureListCheckedProps = {
    features: ReactNode[];
    className?: string;
};
declare const FeatureListChecked: ({ features, className, }: FeatureListCheckedProps) => import("react/jsx-runtime").JSX.Element;
export default FeatureListChecked;
