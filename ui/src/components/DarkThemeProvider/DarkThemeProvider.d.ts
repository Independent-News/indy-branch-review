import { FC } from 'react';
import { DarkThemeProviderProps } from './DarkThemeProvider.types';
/**
 * A theme wrapper that can be used to switch the theme to the dark variant.
 * **NB** It is not possible to switch back to the light theme without using a
 * ThemeProvider with a new theme object.
 */
declare const DarkThemeProvider: FC<DarkThemeProviderProps>;
export default DarkThemeProvider;
//# sourceMappingURL=DarkThemeProvider.d.ts.map